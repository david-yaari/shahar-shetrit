import ContactForm from '@/components/ContactForm';
import Form from '@/components/ContactForm';

const page = () => {
  return (
    <main>
      <ContactForm />
    </main>
  );
};

export default page;
